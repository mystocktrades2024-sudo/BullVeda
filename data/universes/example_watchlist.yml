# Example custom universe — copy this file as data/universes/my_watchlist.yml
# Then run: python3 -m ml.run_ml_edge --universe custom:my_watchlist
#
# Two supported formats:
#   1. Inline list:        tickers: [AAPL, NVDA, MSFT]
#   2. Block list (below):
tickers:
  - AAPL
  - NVDA
  - MSFT
  - TSLA
  - AMD
