{
  "_comment": "Variant A — only allow FRESH entries (within 0.75 ATR of pivot). Drops PULLBACK and VALID. Run via: python3 backtest.py --portfolio --days 250 --config-override config/variants/A_fresh_entries_only.json. Now testable since 2026-05-10 commit f0a66543e propagates entry_quality to trade record.",
  "_baseline_metrics": {
    "Q1_step5_baseline_PF": 0.95,
    "Q1_step5_baseline_WR": 0.317,
    "predicted_A_PF": "1.1-1.3 (modest lift, fewer late-entry whipsaws)",
    "predicted_A_n_trades": "~60% reduction from 142 → ~57"
  },

  "decisions": {
    "_comment": "Variant A — entry_quality filter. Demote any non-FRESH entry to WATCH regardless of score.",
    "entry_quality_required": ["FRESH"]
  },

  "setup_gates": {
    "_comment": "Per-setup hint: only FRESH entries allowed across the board.",
    "default": {"entry_quality_required": ["FRESH"]}
  }
}
