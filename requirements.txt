# SwingTrade runtime dependencies — Python 3.9+
# Install: pip install -r requirements.txt

# ── Data vendors ─────────────────────────────────────────────
requests>=2.31           # HTTP client for Polygon, Finnhub, FMP, Finviz, Slack
yfinance>=0.2.38         # Yahoo fallback + VIX / sector ETF fetches
beautifulsoup4>=4.12     # Finviz HTML parsing  (import: bs4)
pandas>=2.0              # DataFrames, everything
numpy>=1.24              # numeric
pytz>=2024.1             # Market-hours timezone math

# ── Brokerage / execution ─────────────────────────────────────
alpaca-py>=0.30          # Paper-trade bracket orders

# ── Gmail (Zacks newsletter ingestion) ────────────────────────
google-api-python-client>=2.100   # Gmail API  (import: googleapiclient)
google-auth-oauthlib>=1.0         # OAuth flow

# ── Config validation ─────────────────────────────────────────
pydantic>=2.0            # Typed config_schema.py

# ── Web server (FastAPI migration) ────────────────────────────
fastapi>=0.100           # Async API framework (replaces http.server)
uvicorn[standard]>=0.20  # ASGI server for FastAPI

# ── TradingView integration (optional, only if Chart tab used) ─
tvDatafeed>=2.1          # pip install git+https://github.com/StreamAlpha/tvdatafeed
selenium>=4.15           # Headless browser for TV scrapes (optional)
undetected-chromedriver>=3.5      # (optional)

# ── Notes ─────────────────────────────────────────────────────
# Stdlib used extensively: ast, asyncio, concurrent.futures, dataclasses,
# hashlib, http.server, json, logging, pathlib, re, smtplib, subprocess,
# threading, urllib. No install needed for those.
#
# macOS-specific: osascript for Mac notifications (pre-installed).
# launchd for scheduling (pre-installed).
