<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.swingtrade.source-health</string>

    <!-- 2026-06-09 · Open Risk #5 — data-supplier liveness probe.
         Catches the #1 silent-failure: a dead Finviz Elite token silently
         falling back to ~10x EODHD fundamentals cost with NO warning. Also
         probes Schwab OAuth + EODHD quota + free-scrape cache staleness.
         Posts a single Slack CRITICAL on any DOWN; exits 0 always.

         Cadence (PT) — a few times/day in-session + once pre-scan:
         - 05:45  pre-scan (morning briefing runs 06:30 PT)
         - 09:00  mid-morning
         - 12:00  midday
         Each call is cost-trivial (1 cheap probe per supplier). -->

    <key>StartCalendarInterval</key>
    <array>
        <dict><key>Hour</key><integer>5</integer><key>Minute</key><integer>45</integer></dict>
        <dict><key>Hour</key><integer>9</integer><key>Minute</key><integer>0</integer></dict>
        <dict><key>Hour</key><integer>12</integer><key>Minute</key><integer>0</integer></dict>
    </array>

    <key>WorkingDirectory</key>
    <string>/Volumes/MyMacDisk/Claude Skills/SwingTrade</string>

    <key>ProgramArguments</key>
    <array>
        <string>/Volumes/MyMacDisk/Claude Skills/SwingTrade/scripts/launchd_wrapper.sh</string>
        <string>com.swingtrade.source-health</string>
        <string>/usr/bin/python3</string>
        <string>scripts/health_check_sources.py</string>
    </array>

    <key>StandardOutPath</key>
    <string>/Volumes/MyMacDisk/Claude Skills/SwingTrade/cache/logs/source_health.log</string>
    <key>StandardErrorPath</key>
    <string>/Volumes/MyMacDisk/Claude Skills/SwingTrade/cache/logs/source_health.log</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/usr/local/bin:/usr/bin:/bin</string>
    </dict>

    <key>RunAtLoad</key>
    <false/>
</dict>
</plist>
