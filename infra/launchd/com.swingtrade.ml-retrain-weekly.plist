<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.swingtrade.ml-retrain-weekly</string>

    <!-- Saturdays at 3:30 AM PT (2026-06-09: moved from Sunday 03:00). Sits just
         AFTER the Saturday 02:00 weekly-backtest so both CPU-heavy jobs land on
         the same non-trading day, leaving Sunday lighter. Still runs BEFORE the
         Sunday 5pm weekly diagnostics so calibration_report.json is fresh.
         Now registered in launchd_wrapper HEAVY_LABELS — skips if load>8 or a
         scan/other heavy job is running (Open Risk CPU concern). Pipeline:
         close-loop labels → train+gate → Slack alert with full detail. -->
    <key>StartCalendarInterval</key>
    <dict>
        <key>Weekday</key><integer>6</integer>
        <key>Hour</key><integer>3</integer>
        <key>Minute</key><integer>30</integer>
    </dict>

    <key>WorkingDirectory</key>
    <string>/Volumes/MyMacDisk/Claude Skills/SwingTrade</string>

    <key>ProgramArguments</key>
    <array>
        <string>/Volumes/MyMacDisk/Claude Skills/SwingTrade/scripts/launchd_wrapper.sh</string>
        <string>com.swingtrade.ml-retrain-weekly</string>
        <string>/bin/bash</string>
        <string>scripts/ml_weekly_retrain.sh</string>
    </array>

    <key>StandardOutPath</key>
    <string>/tmp/ml-retrain-weekly.log</string>
    <key>StandardErrorPath</key>
    <string>/tmp/ml-retrain-weekly.err</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/usr/local/bin:/usr/bin:/bin</string>
    </dict>

    <key>RunAtLoad</key>
    <false/>
</dict>
</plist>
