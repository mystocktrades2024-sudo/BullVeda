<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.swingtrade.x-signal</string>

    <!-- Poll tracked X handles every 4 hours: 06:00, 10:00, 14:00, 18:00, 22:00 PT.
         Each run polls Nitter for the ~12 tracked handles, refreshes the
         data_x_chatter.json sidecar, and fires post_x_alert.py for any new
         tracked-handle hits on held positions or top-100 scan tickers.
         The 06:00 slot lands just before the morning briefing (06:30) so
         the X CHATTER card on the dashboard is fresh at market open. -->
    <key>StartCalendarInterval</key>
    <array>
        <dict><key>Hour</key><integer>6</integer><key>Minute</key><integer>0</integer></dict>
        <dict><key>Hour</key><integer>10</integer><key>Minute</key><integer>0</integer></dict>
        <dict><key>Hour</key><integer>14</integer><key>Minute</key><integer>0</integer></dict>
        <dict><key>Hour</key><integer>18</integer><key>Minute</key><integer>0</integer></dict>
        <dict><key>Hour</key><integer>22</integer><key>Minute</key><integer>0</integer></dict>
    </array>

    <key>WorkingDirectory</key>
    <string>/Volumes/MyMacDisk/Claude Skills/SwingTrade</string>

    <key>ProgramArguments</key>
    <array>
        <string>/Volumes/MyMacDisk/Claude Skills/SwingTrade/scripts/launchd_wrapper.sh</string>
        <string>com.swingtrade.x-signal</string>
        <string>/Volumes/MyMacDisk/Claude Skills/SwingTrade/scripts/refresh_x_signal.sh</string>
    </array>

    <key>StandardOutPath</key>
    <string>/Volumes/MyMacDisk/Claude Skills/SwingTrade/cache/logs/x_signal_launchd.log</string>
    <key>StandardErrorPath</key>
    <string>/Volumes/MyMacDisk/Claude Skills/SwingTrade/cache/logs/x_signal_launchd.err</string>

    <key>RunAtLoad</key>
    <false/>
</dict>
</plist>
