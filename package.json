{
  "name": "swingtrade-tests",
  "version": "1.0.0",
  "description": "Browser test suite for the V2 dashboard (CapStudio modular system)",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "smoke": "python3 scripts/smoke_test_modules.py"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
